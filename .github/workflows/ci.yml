name: CI

on:
  pull_request:

jobs:
  test-and-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm test

  docker-build-and-smoke-test:
    runs-on: ubuntu-latest
    needs: test-and-lint
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t tracking-validator .

      - name: Run Docker container
        run: docker run -d -p 3000:3000 --name tracking-validator-container tracking-validator

      - name: Smoke test
        run: |
          sleep 5 # Wait for the server to start
          curl -f http://localhost:3000/health

      - name: Stop and remove container
        if: always()
        run: |
          docker stop tracking-validator-container
          docker rm tracking-validator-container
